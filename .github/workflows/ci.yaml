name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  spell-check:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
      - run: make ci-spell-check
